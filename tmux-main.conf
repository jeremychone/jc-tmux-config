
# Prefix Key (or use C-@ "alias" if issues)
set -g prefix C-Space
unbind C-b
bind C-Space send-prefix

# Mouse & Clipboard
set -g mouse on
set -g set-clipboard on

# Others
source-file ~/.config/tmux/keys.conf
source-file ~/.config/tmux/style.conf
source-file ~/.config/tmux/bar.conf
